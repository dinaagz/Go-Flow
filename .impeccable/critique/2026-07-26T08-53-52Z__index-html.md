---
target: index.html
total_score: 33
p0_count: 0
p1_count: 0
timestamp: 2026-07-26T08-53-52Z
slug: index-html
---
Method: dual-agent (A: a882eab11b8481cf2 · B: af3ec38363f7f27cd) — 9ᵉ run, première passe de critique depuis le refactor modulaire (main → index.html thin shell + css/styles.css + 11 js/*.js, mergé sur cette branche entre le round 8 et ce round)

## Score Santé Design

| # | Heuristique | Score | Évolution |
|---|---|---|---|
| 1 | Visibilité de l'état système | 3 | inchangé |
| 2 | Correspondance système / monde réel | 4 | inchangé |
| 3 | Contrôle et liberté utilisateur | 3 | inchangé |
| 4 | Cohérence et standards | **4 (était 3)** | le regroupement du panneau Paramètres (commit `1a5f54c`, appliqué après le round 8) est confirmé présent et bien câblé après le merge |
| 5 | Prévention des erreurs | 4 | inchangé, validation `#p-prix` confirmée intacte post-split |
| 6 | Reconnaissance plutôt que rappel | 3 | inchangé |
| 7 | Flexibilité et efficacité | 3 | inchangé |
| 8 | Esthétique et design minimaliste | 3 | inchangé |
| 9 | Récupération après erreur | 3 | inchangé |
| 10 | Aide et documentation | 4 | inchangé, `KB_SEARCH_ID` confirmé relocalisé correctement |

**Total : 33/40** (était 32/40) — **+1**, entièrement dû au regroupement du panneau Paramètres qui avait atterri entre le round 8 et le merge.

## Verdict AI Slop

Non — round le plus propre de la série en termes de traçabilité : chaque correctif vérifié remonte à un commit réel et identifiable. Le split lui-même (`cca66a0`) est une opération mécanique et vérifiable — zéro dommage collatéral trouvé.

## Vérification post-refactor (le vrai enjeu de ce round)

**Assessment B** — vérification exhaustive :
- `node --check` sur les 11 fichiers `js/*.js` : OK, aucun échec.
- Ordre des balises `<script>` : ordre de dépendance valide. 169 références différées (dans des corps de fonction, jamais exécutées au parse) vers des fichiers chargés plus tard — toutes sûres car `init()` ne s'exécute qu'après le chargement complet des 11 scripts.
- Déclarations dupliquées (`function`/`const`/`let` de premier niveau) : aucune trouvée sur 339 identifiants.
- Tous les correctifs rounds 4-8 confirmés intacts dans leurs nouveaux emplacements : `.ttc-hero`/`.ttc-hero-total` (`css/styles.css`), `KB_SEARCH_ID` avec `devis:'dv-search'` (`js/modals-backup.js`), `#p-prix` requis+validé (`index.html` + `js/catalogue.js`), `#i-alert-triangle` défini et référencé, zéro emoji ⚠️ résiduel.
- Invariants standards (confirm/alert/prompt, échelle z-index, background-clip:text ×2, pas de side-stripe, ARIA progressbar ×3, CSS mort resté supprimé) : tous OK.

**Assessment A** — audit complémentaire de l'ordre de chargement (méthode différente, même conclusion) : aucune fonction appelée avant le chargement de son fichier de définition ; `js/column-manager.js`'s `CM_RENDER` diffère volontairement ses références via des closures fléchées, ce qui n'est pas un hasard mais un choix d'API anticipant la contrainte d'ordre.

**Conclusion : le split manuel a été fait avec un vrai soin. Aucun défaut introduit par la fusion.**

## Forces identifiées

1. Le split-and-replay a été exécuté avec une vraie discipline (ex. `CM_RENDER` conçu pour tolérer l'ordre de chargement).
2. La validation du prix EXW (round 8) a survécu au split avec son intention complète — markup et validation JS ont migré ensemble dans leurs fichiers respectifs.
3. Zéro code mort réintroduit — un risque classique des splits manuels multi-fichiers.

## Priority Issues

**[P2] Cible tactile étroite dans la ligne Trade Assurance du panneau Paramètres** (`index.html` ~ligne 154, `flex:.8` sur l'input numérique dans une rangée à 3 contrôles). Signalé mais **non corrigé ce round** : la hauteur réelle des `.fc` (~40px, `css/styles.css:234`) est cohérente dans toute l'app ; corriger uniquement cette rangée créerait une incohérence sans régler la cause (hauteur globale). Nécessite une vérification visuelle en navigateur réel (non disponible dans cet environnement) avant de toucher au design system global. → `/impeccable adapt` (scope: panneau Paramètres, cibles tactiles) lors d'un futur round avec accès navigateur.

**[P2] Dérive de tailles de police dans le panneau Paramètres** (10.5/11/12/12.5/13/14px groupés dans le texte d'aide et les inputs, hors de l'échelle documentée dans DESIGN.md). Non corrigé ce round pour la même raison (nécessite vérification visuelle). → `/impeccable distill` (scope: `index.html` panneau Paramètres, pattern font-size) lors d'un futur round.

**[P3 — CORRIGÉ] Aucun garde-fou dans le fichier lui-même contre une régression de l'ordre des scripts.** La contrainte de dépendance n'était documentée que dans CLAUDE.md, pas dans `index.html` — un futur contributeur réordonnant (ex. par ordre alphabétique) casserait l'app silencieusement (`ReferenceError` au runtime, pas d'erreur au parse). → Corrigé : commentaire HTML ajouté juste au-dessus du premier `<script src>` renvoyant vers CLAUDE.md.

**[P3] Deux faux positifs du détecteur maintenant confirmés récurrents** (`border-accent-on-rounded` sur `.ph`/`.dv-hdr`, exceptions documentées dans DESIGN.md ; `em-dash-overuse`, tirets français légitimes). Suggestion : les ignorer formellement dans un futur passage plutôt que de les re-trianger à chaque round — mécanisme de config à investiguer (`/impeccable hooks ignore-rule` existe pour le hook post-édition, pas nécessairement pour l'invocation critique).

## Notes détecteur (Assessment B)

`detect.mjs --json index.html css/styles.css js/*.js` (invocation multi-fichiers fonctionne) : 116 findings (58 radius advisory, 38 color advisory, 16 overused-font warning, 2 `border-accent-on-rounded`, 1 `flat-type-hierarchy`, 1 em-dash-overuse). Répartition par fichier : `css/styles.css` 96, `js/devis.js` 9, `js/catalogue.js` 7, `index.html` 4. Faux positif `#005CB3` toujours présent (même cause, maintenant dans `css/styles.css:87`). Aucun serveur de dev disponible — vérification statique uniquement.

## Corrections appliquées (avant présentation de ce rapport)

1. Ajout d'un commentaire HTML au-dessus du bloc `<script src>` documentant que l'ordre est porteur de sens (scripts classiques, portée globale partagée).

Syntaxe validée (`node --check` sur les 11 fichiers `js/*.js`) après cette correction.
