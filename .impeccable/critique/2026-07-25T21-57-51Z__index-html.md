---
target: index.html
total_score: 34
p0_count: 0
p1_count: 0
timestamp: 2026-07-25T21-57-51Z
slug: index-html
---
Method: dual-agent (A: a8f556ff106d7655e · B: a75df2d23268a2463) — 7ᵉ run, après `/impeccable distill` sur le pattern hero-value

## Score Santé Design

| # | Heuristique | Score | Évolution |
|---|---|---|---|
| 1 | Visibilité de l'état système | 3 | inchangé |
| 2 | Correspondance système / monde réel | 4 | inchangé |
| 3 | Contrôle et liberté utilisateur | 3 | inchangé |
| 4 | Cohérence et standards | **4 (était 3)** | **amélioration réelle** — le point signalé 3 rounds de suite (six implémentations indépendantes du pattern hero-value) est résolu : une seule déclaration family/weight/couleur par famille de couleur, tous les sites réduits à un override de taille (et parfois text-align). Vérifié via diff complet, pas seulement l'intention décrite |
| 5 | Prévention des erreurs | 4 | inchangé |
| 6 | Reconnaissance plutôt que rappel | 3 | inchangé |
| 7 | Flexibilité et efficacité | 3 | inchangé |
| 8 | Esthétique et design minimaliste | 3 | inchangé — les deux correctifs du round 6 (`calcPrev()` grid-column:1/-1, `.stat-cat` justify-content:center) toujours en place, aucune régression, mais ce refactor est invisible pour l'utilisateur par nature |
| 9 | Récupération après erreur | 3 | inchangé |
| 10 | Aide et documentation | 4 | inchangé — le nouveau commentaire (lignes 174-180) est un bon exemple du pattern déjà salué au round 6 |

**Total : 34/40** (était 32/40) — **+2**, gain concentré entièrement sur l'heuristique 4, exactement là où la dette était identifiée.

## Verdict AI Slop

Non. Le round le plus propre de la série : les six changements décrits correspondent caractère pour caractère au diff réel (`git show b1ea59f`). Aucun chevauchement de propriété entre la règle partagée et les overrides restants (family/weight/couleur vs font-size/text-align), donc l'exactitude n'est pas juste affirmée mais garantie structurellement par des ensembles de propriétés disjoints — pas par une QA visuelle faillible comme lors des rounds 4-6.

## Forces identifiées

1. **Parité vérifiée, pas juste supposée** — le raisonnement CSS (propriétés disjointes) prouve l'absence de régression visuelle sans avoir besoin d'un rendu réel.
2. **Consolidation gratuite non revendiquée par le commit** — la carte produit (`pr-val pr-ttc`, ligne 2177) et `rowTTC()` du panier (ligne 3741) réutilisaient déjà `.pr-ttc.pr-val` ; elles héritent automatiquement du bénéfice sans aucun changement de code à ces sites.
3. **Le commentaire est honnête sur sa propre limite** — il se limite précisément à "family+weight+couleur... la taille varie légitimement" sans revendiquer plus que ce qu'il fait.

## Priority Issues

**[P2 — CORRIGÉ] Le module d'export image (canvas) réimplémente le même motif sans référence croisée.** `expFitFont(...,'Montserrat,sans-serif',800,...)` (lignes ~5411/5486) recrée à la main "Montserrat 800 + couleur d'accent" pour le nombre qui répond à la question du produit — un `<canvas>` ne peut pas consommer de classe CSS, donc ce n'est pas consolidable littéralement, mais la formulation "single source of truth" du commit pouvait laisser croire que le problème était entièrement clos. → Corrigé : commentaire ajouté aux lignes 174-180 référençant explicitement le module canvas et le besoin de mise à jour manuelle si la règle change.

**[P3] `finalRow` (PDF) : la cellule de label ne déclare pas de `font-family`** — cohérent avec la pratique existante (les autres labels hero non plus), mais c'est le seul label hero sur fond coloré à l'impression, où un fallback de police serait le plus visible. Pas d'action requise immédiatement — signalé pour un futur `/impeccable polish`.

**[P3] Deux règles CSS plutôt qu'une** (`.ttc-hero,...` / `.ttc-hero-total,...`) et chaque site garde son propre override de taille en règle séparée plutôt qu'une custom property partagée (`--hero-size`). Choix légitime (la taille varie réellement selon la densité du contexte, comme l'explique le commentaire), pas un bug — noté pour ne pas confondre "deux règles, cinq sites" avec "une déclaration, zéro variance".

**[P4 / informationnel] Vérification en navigateur réel toujours en attente** — aucun serveur de dev disponible dans cet environnement depuis le round 5 ; le raisonnement CSS (propriétés disjointes) certifie l'absence de régression sans capture d'écran, mais un diff visuel réel resterait la dernière vérification manquante.

## Notes détecteur (Assessment B)

`detect.mjs` : 115 findings (58 `design-system-radius` advisory, 40 `design-system-color` advisory, 16 `overused-font` warning, 1 `em-dash-overuse`), aucun P0/P1. Faux positif `#005CB3` re-confirmé (même cause : parseur DESIGN.md ne lit pas les couleurs imbriquées dans `components:`). Vérification ligne par ligne des 7 points de consolidation (a-g) : tous confirmés, aucune duplication résiduelle trouvée (`grep` sur les deux combos family+weight+couleur ne retourne qu'un seul hit chacun — la règle partagée elle-même). Tous les invariants standards (confirm(), z-index, background-clip:text, side-stripes, debounce, guard clavier, ARIA ×3, is-winner, border-radius:13px, `.stats` mobile, `calcPrev()` grid-column) confirmés OK sans régression. Pas de serveur de dev disponible — vérification uniquement statique.

## Corrections appliquées (avant présentation de ce rapport)

1. Ajout d'une note de référence croisée dans le commentaire du bloc `.ttc-hero`/`.ttc-hero-total` (lignes 174-180) pointant vers le module d'export canvas (`expFitFont`, lignes ~5411/5486), qui réimplémente le même motif indépendamment et devra être mis à jour à la main si la règle CSS change.

Syntaxe JS validée après cette correction.
