---
target: index.html
total_score: 31
p0_count: 0
p1_count: 1
timestamp: 2026-07-25T14-48-27Z
slug: index-html
---
Method: dual-agent (A: a820be88e56ef5a35 · B: ad182cf55bf89eacc) — re-run after the first-pass fixes

## Score Santé Design

| # | Heuristique | Score | Évolution |
|---|---|---|---|
| 1 | Visibilité de l'état système | 3 | inchangé — petit bug d'Escape sur la modale de confirmation trouvé par A et corrigé depuis (voir plus bas) |
| 2 | Correspondance système / monde réel | 4 | inchangé |
| 3 | Contrôle et liberté utilisateur | 3 (était 2) | undo réel sur delProd/delFour/delTrans/clearCart, avec resynchronisation du panier devis |
| 4 | Cohérence et standards | 3 | drift `#0066cc` vs `--bleu-t` trouvé par A dans le PDF devis, corrigé depuis |
| 5 | Prévention des erreurs | 4 (était 3) | restauration de sauvegarde valide le format et affiche les compteurs avant confirmation |
| 6 | Reconnaissance plutôt que rappel | 3 | groupement des sélecteurs de colonnes confirmé en code, pas seulement documenté |
| 7 | Flexibilité et efficacité d'usage | 2 (était 4) | **régression relevée par A** : aucun raccourci clavier nulle part dans l'app, alors que le score précédent avait surtout valorisé la dérogation manuelle du gagnant sans regarder cet angle — score recalibré à la baisse, hors périmètre des correctifs de cette passe |
| 8 | Design esthétique et minimaliste | 4 (était 3) | restructuration de la toolbar confirmée dans le code (3 groupes, dividers), pas cosmétique |
| 9 | Aide à la récupération des erreurs | 3 | `storClearAudit` n'avait pas d'undo malgré la promesse initiale — corrigé depuis |
| 10 | Aide et documentation | 2 | inchangé |
| **Total** | | **31/40** | **Bon (bande 28-35) — +2 points depuis la première critique (29/40)** |

*Note : le score ci-dessus reflète l'évaluation d'Assessment A sur le code juste avant cette synthèse. Trois de ses trouvailles ont été corrigées immédiatement après sa remise (voir "Corrections appliquées suite à cette relecture" plus bas) — le code livré est donc légèrement meilleur que ce que la table capture.*

## Verdict Anti-Patterns

**Toujours non — et la cohérence marque/code est maintenant vérifiée, pas juste affirmée.**

**Évaluation LLM (Assessment A)** : le tally du dégradé (10 usages CSS) correspond exactement à ce que documente désormais DESIGN.md ("4 signés + 6 accents") — un des rares cas où un doc de design system colle réellement au code qu'il décrit. Point notable : A a trouvé **une erreur factuelle dans le doc lui-même** — `.ph` était décrit comme "la bordure de l'espace réservé image catalogue" alors que c'est en réalité l'en-tête d'impression du devis (`@media print` uniquement, `display:none` sinon). Une preuve que même un doc écrit pour clôturer un audit peut contenir une affirmation non vérifiée contre le DOM — corrigé depuis.

**Scan déterministe (Assessment B)** : 113 signalements, structure similaire au scan précédent (bruit de tokens attendu). Toutes les 10 vérifications manuelles demandées reviennent **FIXED** :
- 0 `confirm()` natif restant (6 `askConfirm()` confirmés aux bons emplacements + `toastUndo` câblé sur 4 des 5 flux).
- z-index : 14 valeurs, toutes dans l'échelle documentée.
- `background-clip:text` : toujours exactement 2 (logo, tagline).
- Bande latérale décorative : toujours 0.
- `backdrop-filter` : les 2 badges sur photo (`position:absolute`) correspondent maintenant à la seconde catégorie documentée dans la Règle du Verre Fonctionnel.
- Les 4 couleurs codées en dur (`#fef3c7`, `#92400e`, `#b45309`, `#4ade80`) : 0 occurrence restante, routées vers `--warn`/`--warn-bg`/`--vert`/`--vert-t`.
- `.alt-card.is-winner` : `border-color:var(--vert-t)` confirmé.
- `--violet-t` : défini et utilisé sur l'icône stat concernée.
- `.card-title` : line-clamp à 2 lignes confirmé.
- Restructuration toolbar + `g:` sur les column-pickers catalogue/simulation : confirmée dans le DOM.

## Impression Générale

La deuxième passe confirme que les correctifs de la première critique ne sont pas cosmétiques : le tally du dégradé, l'undo avec resynchronisation du panier, et la restructuration de la toolbar sont vérifiables dans le code, pas seulement revendiqués dans DESIGN.md. La vraie nouveauté de ce tour : une revue de design director a servi de garde-fou sur la documentation elle-même (l'erreur `.ph`), et a détecté deux incohérences fines (le token PDF `#0066cc`, l'undo manquant sur `storClearAudit`) qu'un simple relevé de "case cochée" aurait laissées passer. Les trois ont été corrigées dans la foulée.

## Ce Qui Fonctionne

1. **L'undo restaure l'état complet, pas juste un toast.** `delProd` réinjecte le produit *et* les lignes du panier devis qui le référençaient — une barre plus haute que la plupart des correctifs "ajouter un undo".
2. **Le tally du dégradé (10 usages) correspond exactement aux 4 signés + 6 accents documentés** — la doc a été vérifiée contre le CSS, pas juste réécrite pour paraître résolue (à une exception près, corrigée).
3. **Les jumelles text-safe tiennent leur contraste** : bleu-t 5.34:1, vert-t 5.48:1, violet-t 8.98:1, orange-t 4.72:1, rouge-t 5.26:1 sur blanc — tous au-dessus du seuil AA, vérifiés indépendamment.

## Problèmes Prioritaires

**[P1] Erreur factuelle dans DESIGN.md v3 (trouvée par cette relecture, corrigée)** — `.ph` était décrit comme la bordure de l'espace réservé image catalogue ; c'est en réalité l'en-tête d'impression du devis. **Corrigé** : la description dans DESIGN.md a été mise à jour pour refléter le DOM réel.

**[P2] `storClearAudit` n'avait pas d'undo malgré la promesse initiale (trouvé par cette relecture, corrigé)** — les 4 autres flux avaient `toastUndo`, celui-ci se contentait d'un `askConfirm` avec la mention "irréversible". **Corrigé** : snapshot de `auditHist` avant vidage, restauration via `toastUndo` comme ses cousins.

**[P2] Drift de token dans le PDF devis (trouvé par cette relecture, corrigé)** — `color:#0066cc` sur `prix_ttc` divergeait de `--bleu-t:#006ACC` utilisé partout ailleurs à l'écran. **Corrigé** : remplacé par `var(--bleu-t)`.

**[P3] Échap sur la modale de confirmation ne résolvait pas la promesse (trouvé par cette relecture, corrigé)** — fermait la modale via `closeMod()` générique au lieu de `cfmCancel()`, laissant le `.then()` de l'appelant indéfiniment en attente. **Corrigé**. Un garde a aussi été ajouté sur `cfmResolve` pour éviter qu'un second `askConfirm()` écrase silencieusement le premier.

**[P3] Aucun raccourci clavier nulle part (persistant, hors périmètre de cette passe)** — confirmé par grep, aucun `keydown`/`ctrlKey`/`metaKey` en dehors d'Échap/Tab/flèches d'onglets, dans un outil que CLAUDE.md décrit comme utilisé "huit heures par jour". → `/impeccable optimize` ou `/impeccable adapt`.

## Signaux Persona

**Alex (power user)** : les suppressions n'interrompent plus avec un dialogue navigateur, la toolbar groupée réduit le bruit visuel. Reste ouvert : zéro raccourci clavier ; "Plus ▾" ajoute un clic sur Export image si c'est une action fréquente (à valider avec l'usage réel de l'équipe).

**Sam (prudent sur la perte de données)** : le P0 original (confirm natif, aucun undo, aucun nom d'élément) est réellement corrigé sur les flux principaux — nommer l'élément + undo à délai de grâce est un vrai gain de confiance. L'incohérence `storClearAudit` (undo absent alors que le reste de l'app l'a) était le genre de détail que ce persona remarque et qui érode la confiance dans le motif — corrigée.

**Riley (première utilisation)** : non concerné par cette passe de correctifs — jargon dense (EXW, CBM, TVA, MOQ) toujours sans glossaire, reste un vrai frein pour ce persona.

## Observations Mineures
- `.b-yateli`/`.b-oman` (couleurs badges fournisseurs) ne correspondent plus exactement aux valeurs citées dans CLAUDE.md — probablement du texte resté obsolète après un précédent passage sur les tokens couleur, à resynchroniser à l'occasion.
- Les comptes `design-system-color`/`design-system-radius` du détecteur restent stables (bruit de tokens attendu, non actionnable).

## Corrections appliquées suite à cette relecture
Les 3 trouvailles nouvelles d'Assessment A (erreur `.ph` dans DESIGN.md, undo manquant sur `storClearAudit`, drift `#0066cc`) et le bug d'Échap + le garde `cfmResolve` relevés en observations mineures ont été corrigés immédiatement après cette critique, avant la synthèse ci-dessus.

## Questions à Considérer
- Si `storClearAudit` a maintenant un undo comme ses cousins, la distinction "irréversible" a-t-elle encore un sens ailleurs dans l'app, ou tous les flux destructifs devraient-ils converger vers le même contrat (nommer + undo) par défaut ?
- Export image est-il réellement une action secondaire pour l'équipe, ou a-t-il été déplacé vers "Plus ▾" surtout pour respecter un objectif de charge cognitive — et si c'est le cas, est-ce un compromis qui vaut le coût réel sur le flux de travail ?
- Zéro raccourci clavier dans un outil utilisé 8h/jour : est-ce un chantier à ouvrir maintenant, ou un choix délibéré à documenter comme tel ?
