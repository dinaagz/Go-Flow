---
target: index.html
total_score: 32
p0_count: 0
p1_count: 1
timestamp: 2026-07-25T21-46-30Z
slug: index-html
---
Method: dual-agent (A: a1f39c8ffd8515b5b · B: ac02dfe6204e8c8cb) — 6ᵉ run, après la passe de correction sur les findings du round 5

## Score Santé Design

| # | Heuristique | Score | Évolution |
|---|---|---|---|
| 1 | Visibilité de l'état système | 3 | inchangé |
| 2 | Correspondance système / monde réel | 4 | inchangé |
| 3 | Contrôle et liberté utilisateur | 3 | inchangé |
| 4 | Cohérence et standards | 3 | inchangé — parité 4 sites obtenue sur les cellules TTC de tableau (2286/2647/3840/4130 strictement identiques), mais une **6ᵉ** implémentation indépendante du pattern hero vient d'être ajoutée (`calcPrev()`, 14px) au lieu de réutiliser l'existant |
| 5 | Prévention des erreurs | 4 | inchangé |
| 6 | Reconnaissance plutôt que rappel | 3 | inchangé |
| 7 | Flexibilité et efficacité | 3 | inchangé |
| 8 | Esthétique et design minimaliste | **2 (était 3)** | **régression trouvée ce tour** : le divider ajouté à `calcPrev()` (ligne 2781) ne traversait qu'une demi-largeur sous les réglages par défaut de l'app (7 lignes = nombre impair dans une grille 2 colonnes) ; et `.stat-cat{grid-column:1/-1}` déplaçait le vide externe (tuile orpheline) en vide interne (tuile pleine largeur mais contenu collé à gauche) |
| 9 | Récupération après erreur | 3 | inchangé |
| 10 | Aide et documentation | 4 | inchangé — le commentaire sur `.tbl-alt-row` (lignes 2302-2303) tient la promesse du pattern de documentation |

**Total trouvé au moment du critique : 32/40** (plat vs round 5) — quatre correctifs appliqués, un vrai gain (parité 4-tableaux, intention documentée) entièrement compensé par un nouveau bug et une nouvelle dérive introduits par ces mêmes correctifs.

**Note de transparence** : les deux problèmes P1/P2 ci-dessous ont été corrigés par le parent immédiatement après réception des deux évaluations, avant la présentation de ce rapport (voir "Corrections appliquées" en bas). Le score ci-dessus reflète l'état constaté par les agents, pas l'état actuel du fichier.

## Verdict AI Slop

Non, mais la passe de correction a une vraie régression auto-infligée. Les quatre changements sont ciblés et ancrés dans le code réel. Mais le pattern « le correctif introduit une dérive sœur / ne vérifie pas le rendu réel » (déjà signalé aux rounds 4 et 5) se reproduit une 3ᵉ fois consécutive — ce n'est plus du bruit aléatoire mais une lacune systémique dans la vérification des correctifs avant de les déclarer terminés.

## Forces identifiées

1. **Parité réelle, pas aspirationnelle** sur les 4 cellules TTC de tableau (2286/2647/3840/4130) — style inline strictement identique, vérifié caractère pour caractère.
2. **`.tbl-alt-row` : l'atténuation du TTC est maintenant expliquée**, pas seulement supposée — le commentaire donne une vraie justification de design ("ne pas concurrencer visuellement le gagnant"), résolvant l'incertitude du round 5.
3. **La case de grille vide externe a bien disparu** — `.stats` n'a plus de cellule DOM vide dans la grille à ≤960px.

## Priority Issues

**[P1 — CORRIGÉ] Le nouveau divider de `calcPrev()` ne traversait qu'une demi-largeur.** `#calc-prev-rows` (`.calc-grid`) est en grille 2 colonnes. Avec les réglages par défaut de l'app (`S.trf.val=0`, `S.assu.on=false`), les deux lignes optionnelles (frais transfert, Trade Assurance) ne s'affichent pas, laissant exactement 7 `<div>` — nombre impair — donc la ligne TTC (7ᵉ) tombait seule en colonne 1 et son `border-top` ne barrait que la moitié de la largeur. C'est le rendu **par défaut**, pas un cas limite, et ça touche le prix que tout le panneau existe pour mettre en avant. → Corrigé : ajout de `grid-column:1/-1` sur le conteneur de cette ligne.

**[P2 — ATTÉNUÉ] `.stat-cat{grid-column:1/-1}` déplaçait le problème plutôt que de le résoudre.** `.stat` est en `flex` sans `justify-content`, donc le contenu colle à gauche — faire occuper toute la largeur à `stat-cat` (une statistique volontairement tertiaire, per son propre commentaire CSS 3 lignes au-dessus) créait un grand espace vide à l'intérieur de la tuile, la faisant visuellement rivaliser avec `stat-money`. → Atténué : `justify-content:center` ajouté à `.stat-cat` dans ce breakpoint pour que l'espace se lise comme un centrage voulu plutôt qu'un abandon ; une vraie solution long-terme (3 tuiles secondaires en rangée symétrique) reste à envisager si le temps le permet. → `/impeccable layout` (scope: `.stats` ≤960px, si l'on veut aller plus loin)

**[P2] Une 6ᵉ implémentation indépendante du pattern « hero » vient d'être ajoutée.** Inventaire actuel : `.pr-ttc` (16px), cellules de tableau ×4 (13px), `.sim-total` (24px, or), `.detail-row-hero` (16px), `devis-total-item` inline (22px, or), et maintenant `calcPrev()` (14px) — qui en plus est *moins complète* que les autres : seule la valeur est mise en gras/Montserrat, pas le label (contrairement à `.sim-total`/`.detail-row-hero` qui stylent aussi le label). Signalé en P3 depuis le round 5 sans action ; ce correctif en a ajouté une 6ᵉ au lieu de consolider. → `/impeccable distill` (scope: CSS hero-value — devient prioritaire après 3 rounds de report)

**[P3] Asymétrie label/valeur dans `calcPrev()`.** Contrairement à `.sim-total`/`.detail-row-hero` où le label reçoit aussi un traitement typographique (11px, majuscules, atténué), le label "Prix de Vente TTC:" reste en texte par défaut — seul le divider (maintenant corrigé) signalait que la ligne était spéciale.

**[P3] La cellule TTC du `finalRow` du PDF (index.html:4168) reste une instance partielle du pattern** — gras 15px mais sans override `font-family:'Montserrat'` (hérite de la police de page). Non signalé en P1/P2 par les agents faute d'avoir été touché ce round, mais fait partie du même inventaire à consolider.

## Persona Red Flags

**Alex (power user)** : rencontrait le bug P1 à chaque session "Ajouter un produit" sous les réglages par défaut — corrigé.
**Casey (mobile ≤960px)** : voyait une tuile "Catégories" pleine largeur mais quasi-vide juste sous la tuile argent — atténué par le centrage, mais une vraie hiérarchie à 3 tuiles symétriques resterait plus propre à terme.
**Riley (stress-tester)** : activer/désactiver frais de transfert et Trade Assurance faisait sauter le divider entre colonne 1 et 2 selon la parité des lignes — corrigé par le `grid-column:1/-1`.

## Notes détecteur (Assessment B)

`detect.mjs` : 121 findings (58 `design-system-radius` advisory, 40 `design-system-color` advisory, 22 `overused-font` warning, 1 `em-dash-overuse`), aucun P0/P1 nouveau. Faux positif confirmé : `#005CB3` (ligne 97) signalé "hors DESIGN.md" alors que documenté ligne 80 (couleur imbriquée dans un bloc composant que le parseur du détecteur ne lit pas). Les ~39 autres couleurs `design-system-color` ont été vérifiées individuellement contre le bloc `colors:` de DESIGN.md — elles n'y figurent pas, donc probablement de vraies couleurs ad-hoc non documentées (pas la même classe de faux positif). Tous les invariants standards (zéro `confirm()`, échelle z-index, 2× `background-clip:text`, zéro side-stripe, debounce `dv-search`, guard clavier, ARIA progressbar ×3, zéro `is-winner`, zéro `border-radius:13px`) confirmés OK, sans régression. Pas de serveur de dev disponible dans cet environnement — vérification navigateur non effectuée, uniquement lecture statique du code.

## Corrections appliquées (avant présentation de ce rapport)

1. `calcPrev()` : ajout de `grid-column:1/-1` sur la ligne TTC pour que le divider traverse toute la largeur quel que soit le nombre de lignes affichées.
2. `.stat-cat` (≤960px) : ajout de `justify-content:center` pour que l'espace de la tuile pleine largeur se lise comme un centrage voulu, pas un abandon.

Syntaxe JS validée (`node --check`) après ces deux corrections.
