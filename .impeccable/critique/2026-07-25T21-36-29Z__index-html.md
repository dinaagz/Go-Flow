---
total_score: 32
p0_count: 0
p1_count: 0
timestamp: 2026-07-25T21-36-29Z
slug: index-html
---
Method: dual-agent (A: a479726e35be718ea · B: a6ad04edec21861b4) — 5th run, après les deux passes de correction sur les findings du round 4

## Score Santé Design

| # | Heuristique | Score | Évolution |
|---|---|---|---|
| 1 | Visibilité de l'état système | 3 | inchangé |
| 2 | Correspondance système / monde réel | 4 | la hiérarchie argent-d'abord couvre maintenant 6 des ~8 sites de rendu TTC de l'app |
| 3 | Contrôle et liberté utilisateur | 3 | inchangé |
| 4 | Cohérence et standards | 3 (était 2) | amélioration réelle (panier devis, 4 cellules de tableau, modale détail produit alignés) mais pas un retour complet à 4/4 — `index.html:2320` et `index.html:2779` gardent l'ancien traitement plat, et le fix `.stat-money` introduit un nouveau problème de cohérence (tuile orpheline, cf P2) |
| 5 | Prévention des erreurs | 4 | inchangé |
| 6 | Reconnaissance plutôt que rappel | 3 | inchangé |
| 7 | Flexibilité et efficacité | 3 | inchangé |
| 8 | Esthétique et design minimaliste | 3 | `.stat-money{grid-column:1/-1}` (ligne 132) corrige la troncature mais crée une asymétrie neuve sur exactement le viewport ciblé par le fix |
| 9 | Récupération après erreur | 3 | inchangé |
| 10 | Aide et documentation | 4 (était 3) | les commentaires CSS de justification (lignes 171-172, 256-257, 338-339, 3728-3729) sont maintenant un pattern fiable, pas un one-off |

**Total : 32/40** (était 31/40) — net +1

## Verdict AI Slop

Non. Les deux passes sont ciblées, ancrées dans le code, non-génériques. Les messages de commit correspondent presque entièrement au diff réel. Mais « avoir corrigé les éléments nommés » et « avoir fini le travail » sont deux affirmations différentes : deux surfaces TTC sœurs supplémentaires (une ligne de tableau alternative et le panneau calc-preview du formulaire produit) n'étaient dans le scope d'aucune des deux passes, et le fix P1 de `.stat-money` introduit un nouveau défaut de layout non traité.

## Forces identifiées

1. `rowTTC()` (index.html:3728-3730) réutilise exactement les classes `.pr/.pr-lbl.pr-ttc/.pr-val.pr-ttc` déjà existantes de la carte catalogue — zéro risque de drift entre carte et panier.
2. La régression border-radius du round 4 est bien corrigée — `.stat-money .stat-ico{border-radius:12px}` correspond exactement au radius de base.
3. Suppression propre de `.alt-card.is-winner` — zéro occurrence restante, logique de filtre vérifiée (`others=gps.filter(p=>p.id!==w.id)`).

## Priority Issues

**[P2] `.stat-money{grid-column:1/-1}` (index.html:132) résout la troncature mais crée une tuile orpheline.** À ≤960px la grille passe à 2 colonnes ; money occupe toute la largeur (row 1), puis prod+view forment la row 2, et cat se retrouve seul en row 3 avec une case vide à côté — persiste jusqu'au plus petit téléphone (la query ≤640px ne touche que le `gap`). → `/impeccable layout` (scope: `.stats` à ≤960px)

**[P2] Deux surfaces TTC sœurs n'ont jamais reçu le traitement hero.** (a) `index.html:2320` (ligne alternative du tableau groupé, `<td style="color:var(--bleu-t)">`) — plausiblement une désaccentuation volontaire (ligne "perdante" déjà en `--muted}` ailleurs), à confirmer. (b) `calcPrev()`, `index.html:2779` : `<b style="color:var(--bleu-t)">` — même taille 12px que le reste du bloc, gras navigateur par défaut, pas de Montserrat. Contrevient à « La Règle du Montant » (DESIGN.md). → `/impeccable bolder` (scope: `calcPrev()`)

**[P3] Le pattern « hero » est cohérent mais pas unifié littéralement — cinq implémentations indépendantes** (`.pr-ttc`, cellules de tableau inline, `.sim-total`, `.detail-row-hero`, `devis-total-item` inline) au lieu d'un token/classe partagé. → `/impeccable distill` (scope: hero-value CSS)

**[P3] Ligne 4128 : cellule TTC de table sans `font-size:13px` explicite** (hérite de la taille par défaut du tableau au lieu de déclarer comme ses sœurs 2286/2647/3838).

**[P3] `alt-card-prix` et la ligne alt du tableau groupé sont cohérents entre eux mais l'intention n'est pas documentée** (contrairement aux autres sites hero qui ont un commentaire de justification). → `/impeccable document`

## Notes détecteur (Assessment B)

`detect.mjs` : 120 findings, tous `advisory`/`warning`, aucun P0/P1 nouveau. Faux positif identifié : `#005CB3` (ligne 97) signalé « hors DESIGN.md » alors qu'il est documenté ligne 80 (le parseur du détecteur ne lit pas les couleurs dans le bloc YAML-like). Invariants standards (z-index, `background-clip:text` ×2, pas de side-stripe >1px, debounce `dv-search`, guard clavier, ARIA progressbar) tous confirmés OK. Le "bottom-sheet scrim" mentionné en round 4 n'a pas pu être confirmé comme composant distinct (probablement le même `.overlay` générique).
